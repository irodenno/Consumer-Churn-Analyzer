"""
Run this only after the notebook cell `stack.fit(x_train, y_train)` has completed.

Inside the notebook, the shortest export cell is:

    import joblib
    joblib.dump(stack, "telecom_churn_stack.pkl")

This script is included as a reminder; it cannot access notebook variables
when run as a separate Python process.
"""

print(
    'Open Telecom_churn(1).ipynb and run:\\n'
    'import joblib\\n'
    'joblib.dump(stack, "telecom_churn_stack.pkl")'
)
